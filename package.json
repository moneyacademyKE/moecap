{
    "name": "moecap",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "test": "bun test",
        "test:coverage": "bun test --coverage",
        "dev": "bun run build && bun scripts/serve.ts",
        "start": "bun scripts/serve.ts",
        "build": "bun run scripts/build-site.ts",
        "deploy": "bun run build && bunx wrangler pages deploy public --project-name moecap",
    "nse:research-status": "bun scripts/nse-research-status.ts"
    },
    "devDependencies": {
        "bun-types": "latest"
    }
}